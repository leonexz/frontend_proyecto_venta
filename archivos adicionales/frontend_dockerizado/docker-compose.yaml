services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend_asociacion
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - red-postgres

networks:
  red-postgres:
    external: true
